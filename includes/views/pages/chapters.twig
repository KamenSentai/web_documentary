{% extends 'index.twig' %}

{% block html %}

    {% embed './partials/head.twig' %}
        {% block title %}L'or bleu | Exploration{% endblock %}
    {% endembed %}

    {% embed './partials/body.twig' %}
        {% block body %}
            <div id="barba-wrapper">
                <div class="barba-container">      
                    <h1 class="chapters-title" data-baseurl="{{ base_url() }}/"><a href="{{ base_url() }}/" title="L'or bleu">L'or bleu</a> &#62; Explorer</h1>
                    {% for _chapter in chapters %}
                        <span class="chapters--button">
                            <a href="{{ path_for('chapter', {slug: _chapter.slug}) }}" class="chapters--target">
                                <span class="chapters--span">{{ _chapter.title | raw }}</span>
                            </a>
                        </span>
                    {% endfor %}
                </div>
            </div>
        {% endblock %}
    {% endembed %}

{% endblock %}
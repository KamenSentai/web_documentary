.slider
    position absolute
    left 0
    top 0
    full-size()
    composite(transform, 0.5s, ease-in-out)
    
    .slide
        position relative
        full-size()
        opacity 0
        composite(opacity, 0.5s, ease-in-out)
        &:first-child
            opacity 1
        .area
            position absolute
            top 20vh
            left 3rem
            width calc(100% - 6rem)
            height calc(100% - 20vh - 3rem - 2.5rem - 1.5rem)
            background-color rgba(#f00, 0.5)